MEMORY {
  ROM:     start = $8000, size = $8000, file = %O, fill = yes; 
}
SEGMENTS {
  OS:      load = ROM, type = ro;
  BASIC:   load = ROM, type = ro, start = $9000;
  VECTORS: load = ROM, type = overwrite, start = $FFFA;
}
