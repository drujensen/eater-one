MEMORY {
  RAM:     start = $0000, size = $6000, file = %O;
}
SEGMENTS {
  CODE:    load = RAM, type = ro, start = $1000;
  DATA:    load = RAM, type = rw;
  BSS:     load = RAM, type = bss, define = yes;
}
